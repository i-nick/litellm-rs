//! Rate limiting configuration — re-exported from canonical location.
//!
//! The canonical definition lives in [`crate::core::types::config::rate_limit`].

pub use crate::core::types::config::rate_limit::{RateLimitConfig, RateLimitStrategy};
